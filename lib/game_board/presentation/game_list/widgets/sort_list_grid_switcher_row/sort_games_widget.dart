import 'package:flutter/material.dart';

class SortGamesWidget extends StatelessWidget {
  const SortGamesWidget({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Sort");
    //TODO Implement sort dropdown sprint 2
  }
}
